{
  "name": "@hrms/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --experimental-strip-types --watch src/main.ts",
    "start": "node --experimental-strip-types src/main.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@hrms/contracts": "workspace:*",
    "@hrms/db": "workspace:*",
    "pg-boss": "^12.27.0"
  },
  "devDependencies": { "dotenv": "^17.2.4" }
}
