# Strict: a package may only import what it declares. This is the first line of
# defence for the module-boundary rule in PLAN/12 §3.1 — it makes an undeclared
# cross-package import fail at resolution time, before lint even runs.
hoist=false
strict-peer-dependencies=false
