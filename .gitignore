node_modules/
dist/
.next/
.turbo/
*.tsbuildinfo
.env
.env.local
!.env.example
coverage/
generated/

# Konfigurasi compose berisi kredensial. Contohnya ada di ops/.env.compose.example
ops/.env

# Foto presensi pengembangan. Data pribadi — tidak pernah masuk git.
.storage/

# Cadangan basis data. Memuat seluruh data pelanggan.
backups/
backups-skala

# Editor and assistant tool state. Not project source: it belongs to whoever's
# machine it is on, and committing it means one person's tool session becomes
# everyone's repository history.
.kilo/
